#!/bin/bash

export TARGET=./hack/frozen_node_modules.tar.xz

echo Refreshing ${TARGET}

set -ex

rm -rf ./node_modules
yarn install # --frozen-lockfile
tar -c ./node_modules | xz -9 > ${TARGET}

du -sm ./node_modules ${TARGET}

